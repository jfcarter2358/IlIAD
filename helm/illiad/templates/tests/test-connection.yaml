apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iliad.fullname" . }}-test-connection"
  labels:
    {{- include "iliad.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "iliad.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
