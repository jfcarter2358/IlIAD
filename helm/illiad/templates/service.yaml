apiVersion: v1
kind: Service
metadata:
  name: {{ include "iliad.fullname" . }}
  labels:
    {{- include "iliad.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iliad.selectorLabels" . | nindent 4 }}
